using UnityEngine;

public class CS_PlayerMaterial : MonoBehaviour
{
    [SerializeField] private GameObject playerMesh;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
